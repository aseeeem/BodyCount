#include <stdio.h>
#include <opencv2/opencv.hpp>

using namespace cv;

int main( int argc, char **argv ) {
	
	if( argc != 2 ) {
		sprintf( stderr, "Not enough arugments!" );
		exit(-1);
	}
	Mat image;
return 0;
}
